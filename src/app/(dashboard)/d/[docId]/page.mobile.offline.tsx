"use client";

// Mobile offline document editor. The offline editor is already responsive, so
// for now it reuses the same implementation as the web offline variant. Split
// into a dedicated mobile-tuned layout later if needed.
export { default } from "./page.offline";
